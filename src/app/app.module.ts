import { NgModule } from '@angular/core';
import { AppComponent } from './app.component'; // Import here

@NgModule({
  imports: [
    AppComponent // Import the standalone component
  ],
  // ...
})
export class AppModule {}
